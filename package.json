{
  "name": "correction-reactor",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "action": "bun run src/action.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@warmhub/sdk-ts": "latest",
    "openai": "^4.70.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
